---
server:
  port: 8053
management:
  endpoints:
    web:
      exposure:
        include: health, metrics
  tracing:
    sampling:
      probability: 1
logging:
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]"
